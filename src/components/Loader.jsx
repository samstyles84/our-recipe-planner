import React from "react";
import { StyledSection } from "../styling/styledGlobal";

const Loader = () => {
  return (
    <StyledSection>
      <h3>Loading...</h3>
    </StyledSection>
  );
};

export default Loader;
