import React from "react";
import { StyledHeader } from "../styling/styledGlobal";

const Header = () => {
  return <StyledHeader>Sam and Alex's Meal Planner!</StyledHeader>;
};

export default Header;
